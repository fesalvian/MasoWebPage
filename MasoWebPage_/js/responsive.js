/*document.addEventListener('DOMContentLoaded', () => {
    const hamburgerCheckbox = document.getElementById('label-check');
    const navButtons = document.querySelector('.navButtons');

    hamburgerCheckbox.addEventListener('change', () => {
        if (hamburgerCheckbox.checked) {
            navButtons.style.display = 'flex';  // Exibe os botões de navegação
        } else {
            navButtons.style.display = 'none';  // Esconde os botões de navegação
        }
    });

    // Esconde os botões de navegação por padrão em dispositivos móveis
    if (window.innerWidth <= 768) {
        navButtons.style.display = 'none';
    }
});*/

